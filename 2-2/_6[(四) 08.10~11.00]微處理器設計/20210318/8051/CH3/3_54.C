/************* 3_54.c ************************
*動作：LED遞加輸出，含延時函數
**********************************************/
#include "..\AT89X52.H"  //暫存器及組態設定
sfr    LED=0x80; //P0為LED輸出
void delay(char count)	//將10存入count內，但不傳回值
{
   while(count--);  //count遞減到0才退出函數
}
/********************************************/
main()  //主程式
{
 unsigned char i=0;//計數值
  loop:
    LED=i;	   //計數值由LED輸出
    delay(10); //呼叫自訂延時函數
    i++;	   //計數值遞加
    goto loop;
}
