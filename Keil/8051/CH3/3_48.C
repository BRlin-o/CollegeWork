/************* 3_48.c ************************
*動作：令由LED=1~7輸出，若超過則停止
**********************************************/
#include "..\AT89X52.H"  //暫存器及組態設定
sfr    LED=0x80; //P0為LED輸出	
main()
{
  char  i=1;   //宣告計數初值
  do
    {
      LED=i;   //計數由LED輸出 
      Delay_ms(100); //延時
      i++;     //計數遞加
    }
  while (i<8); //若計數<8，則跳到do執行
  while(1);    //若計數=8，自我空轉
}
